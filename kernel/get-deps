#! /bin/sh

set -ex

cd "$(dirname "$0")"

: "${BUILD_DIR:=../build/kernel}"

mkdir -p "$BUILD_DIR/include"

curl -Lo "$BUILD_DIR/include/limine.h"  https://raw.githubusercontent.com/Limine-Bootloader/limine-protocol/trunk/include/limine.h

touch "$BUILD_DIR/.deps-obtained"
